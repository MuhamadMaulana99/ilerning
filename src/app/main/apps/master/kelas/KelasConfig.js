import Kelas from './pages/Kelas';

const KelasConfig = {
  settings: {
    layout: {},
  },
  routes: [
    {
      path: '/apps/masterkelas/',
      element: <Kelas />,
    },
  ],
};

export default KelasConfig;
